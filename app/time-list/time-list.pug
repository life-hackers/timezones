time-list(class='{ css.host }')
    time-item(each='{ time, i in list }'
        on-event='{ itemEvent }'
        time='{ time }' at='{ at }')
    script.
        import './time-item.pug'
        import { TimeList } from './time-list'
        new TimeList(this, opts)